rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // FULLY OPEN RULES - This removes all permission barriers for your property solutions hub.
      allow read, write: if true;
    }
  }
}