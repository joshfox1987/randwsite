rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read and write for gallery images to unblock the user
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true; 
    }
  }
}