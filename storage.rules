rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all files so the gallery can display them
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
