rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // FULLY OPEN RULES - This ensures all project photos can be uploaded and viewed without restriction.
      allow read, write: if true;
    }
  }
}